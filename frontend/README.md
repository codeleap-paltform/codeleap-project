# 码跃前端

Vue 3、Vite、Vue Router、Axios 和 Element Plus 构建的任务管理前端。

## 本地运行

1. 复制 `.env.example` 为 `.env.local`，按需修改后端地址。
2. 运行 `npm install`。
3. 运行 `npm run dev`。

后端默认地址为 `http://localhost:8080`。登录使用服务端会话Cookie，前后端地址必须与后端CORS配置一致。
